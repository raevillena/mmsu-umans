# ============================================
# Server Configuration
# ============================================
# Port on which the server will run
PORT=3001

# API base URL (used for Swagger docs and email links)
API_URL=localhost

# Domain URL (used for password reset links)
DOMAIN_URL=localhost:3001

# Debug mode (set to 'true' for development, 'false' for production)
DEBUG=true

# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# CORS Configuration
# ============================================
# Development allowed origins (single origin)
ALLOWED_ORIGINS_DEV=http://localhost:5173

# Production allowed origins (comma-separated list)
# Example: ALLOWED_ORIGINS_PROD=https://example.com,https://www.example.com
ALLOWED_ORIGINS_PROD=https://yourdomain.com

# ============================================
# Database Configuration (MariaDB/MySQL)
# ============================================
DB_USER=root
DB_PASS=your_password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=umans_db

# ============================================
# Redis Configuration
# ============================================
# Redis connection URL
# Format: redis://[username:][password@]host:port
# 
# Examples:
#   - No authentication: redis://localhost:6379
#   - With password: redis://:your_password@localhost:6379
#   - With username and password: redis://username:password@localhost:6379
#   - Remote with password: redis://:password@128.168.100.2:6379
#
# NOTE: If you get "NOAUTH Authentication required" error, 
#       your Redis server requires a password. Include it in the URL above.
REDIS_URL=redis://localhost:6379

# ============================================
# Google OAuth Configuration
# ============================================
# Google OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your_google_client_id_here

# Google OAuth 2.0 Client Secret
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Google OAuth Redirect URL
# This should match the redirect URI configured in Google Cloud Console
GOOGLE_REDIRECT_URL=http://localhost:3001/api/auth/google/callback

# ============================================
# Email Service Configuration
# ============================================
# Email service username (Gmail account)
EMAIL_USER=your_email@gmail.com

# Email service password (Gmail App Password)
# Note: Use App Password, not your regular Gmail password
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_PASS=your_app_password_here

# ============================================
# Security & Token Configuration
# ============================================
# Password reset token expiry time in milliseconds
# Default: 3600000 (1 hour)
PASSWD_RESET_TOKEN_EXPIRY=3600000

# ============================================
# Notes
# ============================================
# 1. Copy this file to .env in the backend directory
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit .env file to version control
# 4. Keep your .env file secure and private
# 5. For production, use strong passwords and secure values
# 6. Ensure Redis and MariaDB services are running before starting the server
# 7. If Redis requires authentication, include password in REDIS_URL
